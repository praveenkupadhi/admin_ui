# Administrator User Experience

React-based administrator interface for member management (Redux, React Router, MUI/styled-components).

## Scripts

- `npm start` — run the app in development
- `npm run build` — production build
- `npm test` — run tests
